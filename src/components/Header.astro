---
const pathname = Astro.url.pathname;

const serviceLinks = [
  { href: '/services/water-management', label: "Gestió d'Aigua" },
  { href: '/services/environmental', label: 'Gestió Ambiental' },
  { href: '/services/engineering', label: 'Enginyeria' },
];

const isServicesActive = pathname.startsWith('/services');

function isActive(href: string): boolean {
  if (href === '/') return pathname === '/';
  return pathname.startsWith(href);
}
---

<header class="bg-white border-b border-stone-200 sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">

      <!-- Logo — Lora serif mark -->
      <a href="/" class="font-display text-xl italic font-medium text-brand-700 hover:text-brand-500 no-underline transition-colors">
        Àbiòtic
      </a>

      <!-- Desktop nav -->
      <nav class="hidden md:flex items-center gap-7">

        <a
          href="/"
          class:list={[
            'text-[11px] font-semibold uppercase tracking-widest no-underline transition-colors pb-0.5',
            isActive('/')
              ? 'text-brand-700 border-b border-brand-500'
              : 'text-stone-500 hover:text-brand-700 border-b border-transparent hover:border-brand-300'
          ]}
        >
          Inici
        </a>

        <a
          href="/company"
          class:list={[
            'text-[11px] font-semibold uppercase tracking-widest no-underline transition-colors pb-0.5',
            isActive('/company')
              ? 'text-brand-700 border-b border-brand-500'
              : 'text-stone-500 hover:text-brand-700 border-b border-transparent hover:border-brand-300'
          ]}
        >
          L'empresa
        </a>

        <!-- Services dropdown -->
        <div class="relative group">
          <button
            id="services-menu-btn"
            aria-haspopup="true"
            aria-expanded="false"
            aria-controls="services-menu"
            class:list={[
              'flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-widest transition-colors pb-0.5',
              isServicesActive
                ? 'text-brand-700 border-b border-brand-500'
                : 'text-stone-500 hover:text-brand-700 border-b border-transparent hover:border-brand-300'
            ]}
          >
            Serveis
            <svg class="w-3 h-3 mt-px opacity-60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>

          <!-- Dropdown — square corners, clean border, bridge fix -->
          <div
            id="services-menu"
            role="menu"
            class="absolute top-full left-0 mt-1 w-52 bg-white border border-stone-200 shadow-sm hidden group-hover:block group-focus-within:block z-10 before:absolute before:content-[''] before:h-2 before:w-full before:-top-2 before:left-0"
          >
            <!-- Top accent bar -->
            <div class="h-px w-full bg-brand-500"></div>
            {serviceLinks.map(({ href, label }) => (
              <a
                href={href}
                role="menuitem"
                class:list={[
                  'block px-5 py-3 text-[11px] font-semibold uppercase tracking-widest no-underline transition-colors border-l-2',
                  isActive(href)
                    ? 'text-brand-700 bg-brand-50 border-l-brand-500'
                    : 'text-stone-600 hover:text-brand-700 hover:bg-stone-50 border-l-transparent hover:border-l-brand-300'
                ]}
              >
                {label}
              </a>
            ))}
          </div>
        </div>

        <!-- Contacte — outlined CTA button -->
        <a
          href="/contact"
          class:list={[
            'text-[11px] font-semibold uppercase tracking-widest no-underline transition-colors px-4 py-1.5 border',
            isActive('/contact')
              ? 'bg-brand-700 text-white border-brand-700'
              : 'text-brand-700 border-brand-700 hover:bg-brand-700 hover:text-white'
          ]}
        >
          Contacte
        </a>

      </nav>

      <!-- Mobile: checkbox hamburger (CSS-only) -->
      <label for="mobile-menu" class="md:hidden cursor-pointer p-2 text-stone-500 hover:text-brand-700 transition-colors" aria-label="Obrir menú">
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </label>
      <input type="checkbox" id="mobile-menu" class="hidden peer/menu" />
    </div>
  </div>

  <!-- Mobile nav panel — typographic list, no rounded corners -->
  <div class="hidden peer-checked/menu:block md:hidden border-t border-stone-100 bg-white">
    <nav class="max-w-6xl mx-auto px-4 py-2 flex flex-col text-[11px] font-semibold uppercase tracking-widest">

      <a
        href="/"
        class:list={[
          'px-2 py-3.5 no-underline border-b border-stone-100 transition-colors',
          isActive('/') ? 'text-brand-700' : 'text-stone-500 hover:text-brand-700'
        ]}
      >
        Inici
      </a>

      <a
        href="/company"
        class:list={[
          'px-2 py-3.5 no-underline border-b border-stone-100 transition-colors',
          isActive('/company') ? 'text-brand-700' : 'text-stone-500 hover:text-brand-700'
        ]}
      >
        L'empresa
      </a>

      <!-- Services group label -->
      <div class="px-2 pt-4 pb-1.5 text-[9px] text-stone-300 uppercase tracking-[0.25em]">Serveis</div>
      {serviceLinks.map(({ href, label }) => (
        <a
          href={href}
          class:list={[
            'px-2 py-3 no-underline border-b border-stone-100 transition-colors pl-5',
            isActive(href) ? 'text-brand-700' : 'text-stone-500 hover:text-brand-700'
          ]}
        >
          {label}
        </a>
      ))}

      <a
        href="/contact"
        class:list={[
          'mx-2 my-4 px-4 py-3 border text-center no-underline transition-colors',
          isActive('/contact')
            ? 'bg-brand-700 text-white border-brand-700'
            : 'text-brand-700 border-brand-700 hover:bg-brand-700 hover:text-white'
        ]}
      >
        Contacte
      </a>

    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById('services-menu-btn');
  const menu = document.getElementById('services-menu');
  if (btn && menu) {
    const setExpanded = (val: boolean) => btn.setAttribute('aria-expanded', String(val));
    btn.addEventListener('mouseenter', () => setExpanded(true));
    btn.closest('.group')?.addEventListener('mouseleave', () => setExpanded(false));
    btn.addEventListener('focus', () => setExpanded(true));
    menu.addEventListener('focusout', (e) => {
      if (!menu.contains((e as FocusEvent).relatedTarget as Node)) setExpanded(false);
    });
  }
</script>
